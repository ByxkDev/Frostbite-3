# BF4 Blaze Emulator

[![Status](https://img.shields.io/badge/status-WIP-orange)](https://github.com/Aim4kill/BlazeSDK)
[![Language](https://img.shields.io/badge/language-Go-blue)](https://go.dev/)
[![Platform](https://img.shields.io/badge/platform-PS3-lightgrey)](https://www.playstation.com/)

---

# ⚠️ Work In Progress

This project is an experimental implementation of a replacement backend for **Battlefield 4 (PlayStation 3)**.

The primary goal is to recreate the required EA backend infrastructure so that the original Battlefield 4 PS3 client can communicate with a custom server **without modifying the game executable**.

The emulator currently provides:

* DNS-based redirection
* A TLS-enabled GOS Redirector endpoint
* A Blaze TCP game server
* Blaze packet parsing
* TDF parsing
* Component-based request handling
* Custom certificate generation
* Legacy TLS compatibility
* Blaze `preAuth`
* Blaze `ping`
* Initial `FetchClientConfig` implementation

The major TLS compatibility blocker has been resolved.

The original Battlefield 4 PS3 client can successfully complete the TLS handshake with the custom server using the certificate generated by the included `generate.bat` script.

Development is now focused on reproducing the Blaze application traffic required by the original PS3 client.

---

# Current Status

The TLS handshake is **working**.

The original Battlefield 4 PS3 client can now connect to the custom backend and continue into the Blaze application protocol.

The current server architecture is:

```text
Battlefield 4 PS3
        |
        | TLS
        v
gosredirector.ea.com:42127
        |
        | Redirect / Blaze response
        v
151.xxx.xxx.xx:33152
        |
        | Blaze TCP
        v
Custom BF4 Blaze Server
```

The current Blaze flow has progressed beyond the initial connection stage:

```text
DNS Redirection
        |
        v
GOS Redirector
        |
        v
GetServerInstance
        |
        v
Blaze Server
        |
        v
preAuth
        |
        v
ping
        |
        v
FetchClientConfig
        |
        v
Additional Blaze Services
```

`preAuth` and `ping` are currently working. Development is now focused on `FetchClientConfig` and the remaining configuration/service requests generated by the original PS3 client.

---

# TLS Status

The Battlefield 4 PS3 client successfully:

* Connects to the custom redirector.
* Sends a TLS ClientHello.
* Negotiates a compatible legacy TLS configuration.
* Accepts the generated certificate.
* Completes the TLS handshake.
* Continues to the Blaze application protocol.

The emulator supports the legacy RSA cipher suites required by the PS3 client.

Current TLS configuration includes:

```text
TLS 1.0 - TLS 1.2

TLS_RSA_WITH_AES_128_CBC_SHA
TLS_RSA_WITH_AES_256_CBC_SHA
```

The TLS implementation is designed around the limitations of EA's legacy **ProtoSSL** implementation used by Battlefield 4 PS3.

---

# Certificate Generation

The repository contains the certificate generation tools required for the PS3 TLS connection.

Certificate files are located under:

```text
network/certificates/
```

The server expects:

```text
network/certificates/gosredirector.pfx
```

**The certificate is not included in this repo. Use the Bug_OldProtoSSL guide from Aim4Kill's GitHub.**

The certificate is loaded from the PFX file at runtime.

The server extracts the certificate and RSA private key using PowerShell and converts them into the format required by Go's TLS implementation.

---

# DNS Redirection

The original game connects to:

```text
gosredirector.ea.com
```

DNS is used to redirect the hostname to the emulator server.

Example:

```text
gosredirector.ea.com -> 151.xxx.xxx.xx
```

The original hostname is preserved so that the PS3 client can continue communicating using the expected EA endpoint.

No EBOOT modification is required for the DNS redirection itself.

---

# No Client Binary Modification

One of the primary goals of this project is maintaining compatibility with the original Battlefield 4 PS3 client.

The emulator is designed to operate without:

* Modified EBOOT files
* Patched executables
* Altered game assets
* Custom network libraries

Instead, compatibility is provided through:

* DNS redirection
* TLS compatibility
* Protocol recreation
* Blaze packet parsing
* TDF parsing
* Custom backend services

---

# Server Architecture

The emulator currently consists of two primary TCP services.

## GOS Redirector

```text
Hostname:
gosredirector.ea.com

Port:
42127

Protocol:
TLS/TCP
```

The redirector uses:

```text
network/certificates/gosredirector.pfx
```

The server loads the PFX certificate and starts a TLS listener.

Connections are passed into the Blaze packet handler after the TLS layer has been established.

---

## Blaze Server

```text
IP:
151.xxx.xxx.xx

Port:
33152

Protocol:
TCP
```

The Blaze server uses a normal TCP listener.

Unlike the redirector, the current server listener does not use TLS.

Incoming connections are passed directly to the Blaze packet handler.

---

# Blaze Protocol

The emulator implements portions of EA's Blaze networking protocol.

Incoming TCP data is passed to the Blaze component handler:

```go
reply := components.HandlePacket(data)
```

The general packet flow is:

```text
PS3 Client
    |
    | TCP
    v
Server
    |
    v
Blaze Packet Parser
    |
    v
TDF Decoder
    |
    v
Component Handler
    |
    v
Response Builder
    |
    v
PS3 Client
```

The server currently receives Blaze traffic and passes it to:

```go
components.HandlePacket()
```

Responses generated by the component handler are written directly back to the client.

---

# Services

## GOS Redirector

```text
gosredirector.ea.com:42127
```

Responsibilities:

* TLS connection
* Initial EA redirector communication
* GetServerInstance
* Blaze packet handling
* Server redirection

---

## Blaze Server

```text
151.xxx.xxx.xx:33152
```

Responsibilities:

* Blaze TCP communication
* Packet handling
* Component requests
* Blaze responses
* preAuth
* ping
* Client configuration
* Future authentication/session handling

---

# Project Structure

```text
Frostbite 3/

├── Battlefield 4/
├── blaze/   
│   ├── packet.go
│   └── tdf.go
│
├── components/
│   ├── components.go
│   └── ...
│
├── network/
│   └── certificates/
│       ├── generate.bat
│       └── gosredirector.pfx
│
└── main.go
```

---

# Requirements

* Go 1.20+
* Battlefield 4 PS3 v1.20
* Windows PowerShell
* DNS server capable of custom records
* Server/VPS with TCP support
* Port `42127`
* Port `33152`

---

# Running the Server

Generate the required certificate using:

```text
network/certificates/generate.bat
```

Make sure the generated PFX exists at:

```text
network/certificates/gosredirector.pfx
```

Then start the emulator:

```text
go run .
```

Expected output:

```text
Redirector : gosredirector.ea.com:42127
Blaze: 151.xxx.xxx.xx:33152
Certificate: network/certificates/gosredirector.pfx
Waiting for PS3 connections...
```

When the PS3 connects:

```text
[REDIRECTOR] Client connected: ...
```

The server then processes the incoming Blaze data through:

```go
components.HandlePacket()
```

---

# Client Analysis

Development relies heavily on information extracted from the original Battlefield 4 PlayStation 3 client.

A `strings.txt` file can be generated from the original `EBOOT.ELF`.

These strings help identify:

* Domains
* URLs
* Blaze components
* Service names
* Redirect endpoints
* Internal protocol identifiers
* Configuration values

This information is combined with:

* Packet captures
* TLS debugging
* Server logs
* PS3 client logs
* Protocol analysis

to reproduce the behavior expected by the original client.

---

# Current Roadmap

1. ~~Complete TLS handshake.~~
2. ~~Implement GOS Redirector server discovery.~~
3. ~~Implement Blaze TCP server.~~
4. ~~Implement `preAuth`.~~
5. ~~Implement `ping`.~~
6. **Complete `FetchClientConfig`.**
7. Analyze and implement remaining Util commands.
8. Complete authentication flow.
9. Implement session management.
10. Implement matchmaking.
11. Implement presence.
12. Implement game reporting.
13. Continue protocol reverse engineering.
14. Expand Blaze component coverage.

---

# Not Yet Implemented

* Complete `FetchClientConfig` coverage
* Remaining Util commands
* Complete authentication flow
* Session management
* Matchmaking
* Presence
* Game reporting
* Statistics backend
* Leaderboards
* Friends
* Clubs
* Complete Blaze component coverage
* Full Battlefield 4 backend functionality

---

# Project Status

| Component                          | Status        |
| ---------------------------------- | ------------- |
| DNS Redirection                    | ✅ Working     |
| Certificate Generation             | ✅ Working     |
| TLS Redirector Listener            | ✅ Working     |
| TLS ClientHello Reception          | ✅ Working     |
| TLS Handshake                      | ✅ Working     |
| Legacy ProtoSSL Compatibility      | ✅ Working     |
| Blaze TCP Listener                 | ✅ Working     |
| Blaze Packet Parser                | ✅ Working     |
| TDF Parser                         | ✅ Working     |
| Component Handler                  | ✅ Working     |
| GOS Redirector / GetServerInstance | ✅ Working     |
| Blaze `preAuth`                    | ✅ Working     |
| Blaze `ping`                       | ✅ Working     |
| `FetchClientConfig`                | ⏳ In Progress |
| Remaining Util Commands            | ⏳ In Progress |
| Authentication                     | ⏳ In Progress |
| Session Management                 | ⏳ Planned     |
| Matchmaking                        | ⏳ Planned     |
| Presence                           | ⏳ Planned     |
| Game Reporting                     | ⏳ Planned     |

---

# Development Milestone

The project has now passed the initial networking and Blaze connection milestones.

The original Battlefield 4 PS3 client can:

```text
DNS Redirection
       |
       v
gosredirector.ea.com:42127
       |
       v
TLS Handshake
       |
       v
GetServerInstance
       |
       v
151.xxx.xxx.xx:33152
       |
       v
Blaze Connection
       |
       v
preAuth
       |
       v
ping
       |
       v
FetchClientConfig
       |
       v
Additional Blaze Services
```

The TLS layer is no longer the primary blocker.

The current development focus is the **Blaze application protocol**, specifically reproducing the configuration and service requests required by the original Battlefield 4 PS3 client.

---

# Purpose

This project exists for:

* Game preservation
* Reverse engineering research
* Network protocol research
* Learning about legacy online architectures
* Educational purposes

The goal is to understand the original Battlefield 4 backend architecture and develop a compatible replacement implementation.

---

# Disclaimer

This project is an independent implementation created for research and preservation purposes.

It is **not affiliated with, endorsed by, or associated with Electronic Arts or DICE**.

Battlefield and all related trademarks are the property of their respective owners.

---

# Contributing

Contributions are welcome.

If you are interested in helping with:

* Blaze protocol research
* Packet analysis
* TLS compatibility
* Reverse engineering
* Development
* Documentation
* Testing

feel free to reach out on Discord:

**@Byxk**

Any assistance is greatly appreciated.

---

# License

This project is intended solely for:

* Research
* Education
* Preservation

No original EA server software is included.

Only original source code written for this emulator is distributed.

---

# Current Development Focus

> **TLS, GetServerInstance, preAuth, and ping are working. The current focus is implementing and analyzing FetchClientConfig and the remaining Blaze Util/service traffic required by the original Battlefield 4 PS3 client.**
